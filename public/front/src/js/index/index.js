jQuery(document).ready(function($) {

    /*var jssor_1_options = {
        $AutoPlay: 1,
        $Idle: 0,
        $SlideDuration: 5000,
        $SlideEasing: $Jease$.$Linear,
        $PauseOnHover: 4,
        $SlideWidth: 140,
        $Cols: 6
    };

    var jssor_1_slider = new $JssorSlider$("jssor_1", jssor_1_options);

    function ScaleSlider() {
        var refSize = jssor_1_slider.$Elmt.parentNode.clientWidth;
        var he = jssor_1_options.$SlideHeight;
        if (refSize > 1079) {
            refSize = Math.min(refSize, 790);
            jssor_1_slider.$ScaleWidth(refSize);
        }
        else if (refSize < 1079 && refSize > 892) {
            refSize = Math.min(refSize, 620);
            jssor_1_slider.$ScaleWidth(refSize);
        }
        else if (refSize < 892 && refSize > 881) {
            refSize = Math.min(refSize, 950);
            console.log(refSize);
            jssor_1_slider.$ScaleWidth(refSize);
        }
        else if (refSize < 881 && refSize > 700) {
            refSize = Math.min(refSize, 735);
            jssor_1_slider.$ScaleWidth(refSize);
            console.log(he);
            jssor_1_slider.$ScaleHeight(400);
        }
        else if (refSize < 320 && refSize > 480) {
            refSize = Math.min(refSize, 620);
            jssor_1_slider.$ScaleWidth(refSize);
        }
        else {
            window.setTimeout(ScaleSlider, 30);
        }
    }
    ScaleSlider();
    $(window).bind("load", ScaleSlider);
    $(window).bind("resize", ScaleSlider);
    $(window).bind("orientationchange", ScaleSlider);
    */





   /* var jssor_1_options = {
        $AutoPlay: 1,
        $DragOrientation: 2,
        $PlayOrientation: 2,

        $ArrowNavigatorOptions: {
            $Class: $JssorArrowNavigator$,
            $AutoCenter:1
        }
    };

    var jssor_1_slider = new $JssorSlider$("jssor_1", jssor_1_options);

    var jssor_1_options = {
        $AutoPlay: 1,
        $DragOrientation: 2,
        $PlayOrientation: 2,
        $ArrowNavigatorOptions: {
            $Class: $JssorArrowNavigator$,
            $AutoCenter:1
        }
    };
    var jssor_1_slider = new $JssorSlider$("jssor_1", jssor_1_options);
    function ScaleSlider() {
        var refSize = jssor_1_slider.$Elmt.parentNode.clientWidth;
        var he = jssor_1_options.$SlideHeight;
        if (refSize > 1079) {
            refSize = Math.min(refSize, 790);
            jssor_1_slider.$ScaleWidth(refSize);
        }
        else if (refSize < 1079 && refSize > 892) {
            refSize = Math.min(refSize, 620);
            jssor_1_slider.$ScaleWidth(refSize);
        }
        else if (refSize < 892 && refSize > 881) {
            refSize = Math.min(refSize, 950);
            console.log(refSize);
            jssor_1_slider.$ScaleWidth(refSize);
        }
        else if (refSize < 881 && refSize > 700) {
            refSize = Math.min(refSize, 735);
            jssor_1_slider.$ScaleWidth(refSize);
            console.log(he);
            jssor_1_slider.$ScaleHeight(400);
        }
        else if (refSize < 320 && refSize > 480) {
            refSize = Math.min(refSize, 620);
            jssor_1_slider.$ScaleWidth(refSize);
        }
        else {
            window.setTimeout(ScaleSlider, 30);
        }
    }
    ScaleSlider();
    $(window).bind("load", ScaleSlider);
    $(window).bind("resize", ScaleSlider);
    $(window).bind("orientationchange", ScaleSlider);

    $('.jssora08l').prependTo('.sliderhome');
    $('.jssora08r').prependTo('.sliderhome');*/
});